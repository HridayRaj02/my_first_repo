# Login UI (React + Vite)

Minimal React + TypeScript login page matching the provided design.

## Getting started

```bash
npm install
npm run dev
```

Open the printed local URL to view the page. Use `npm run build` to produce a production build and `npm run preview` to serve it locally.
